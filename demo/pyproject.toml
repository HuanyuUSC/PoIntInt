[project]
name = "pointint-demo"
version = "0.1.0"
description = "PoIntInt intersection volume visualization demo"
requires-python = ">=3.9"
dependencies = [
    "viser>=0.2.0",
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
